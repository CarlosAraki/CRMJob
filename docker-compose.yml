services:
  crm-job:
    build: .
    container_name: crm-busca-emprego
    ports:
      - "9991:9991"
    volumes:
      - crm-data:/app/data
    environment:
      - DB_PATH=/app/data/crm_job.db
      - UPLOADS_PATH=/app/data/uploads
    restart: unless-stopped

volumes:
  crm-data:
